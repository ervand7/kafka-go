 Metrics — expose /metrics and view totals in Grafana.
 Scaling — docker compose up -d --scale reporter=3 to fan-out processing.





Next possible enhancements:

Monitoring: count DLQ writes per minute in Prometheus.
Retry topic: after human fix, move messages to orders-retry for replay.
Exactly-once flow with idempotent producers.
